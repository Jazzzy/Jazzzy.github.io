$( document ).ready( onReadyDocument );


function onReadyDocument() {
  setUpParallax();
}


function setUpParallax() {
  $( ".parallax" ).parallax();

  //Read here width and heigth of the window and update the heigth and width of the parallax-container and parallax-image.
}
